--- Makefile	2019-03-29 08:30:30.000000000 -0400
+++ Makefile_add_curl_opts	2019-05-27 16:19:00.000000000 -0400
@@ -2,7 +2,7 @@
 MANDIR ?= $(PREFIX)/share/man
 BUILDDIR=build
 CMAKEMAKE=$(BUILDDIR)/Makefile
-CMAKEOPTS=-DCMAKE_INSTALL_PREFIX:PATH=$(PREFIX) -DCMAKE_INSTALL_MANDIR:PATH=$(MANDIR)
+CMAKEOPTS=-DCMAKE_INSTALL_PREFIX:PATH=$(PREFIX) -DCMAKE_INSTALL_MANDIR:PATH=$(MANDIR) $(CURL_OPTS)
 
 all: $(CMAKEMAKE)
 	$(MAKE) -C $(BUILDDIR) all
