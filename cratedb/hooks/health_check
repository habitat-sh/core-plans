#!/bin/sh

raw_data="$(wget -qO - "{{sys.ip}}:{{cfg.http.port}}" | grep "ok")"
status="$(echo "${raw_data: : -1}" | awk '{print $3}')"

case $status in
    "true")
	rc=0 ;;
    "false")
	rc=2 ;;
    *)
	rc=3 ;;
esac

exit $rc
