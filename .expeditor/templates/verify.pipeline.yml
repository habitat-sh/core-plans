expeditor:
  defaults:
    buildkite:
      timeout_in_minutes: 10
      retry:
        automatic:
          limit: 1

steps:
  - label: "Testing"
    command: ".expeditor/soft_fail.sh"
    soft_fail:
      - exit_status: 52
