# Accept connections on the specified port, default is 6379.
# If port 0 is specified Redis will not listen on a TCP socket.
port = '6379'
password = 'password'
loglevel = 'notice'

bootstrap_from_backup = false
shield_endpoint = ''
shield_skip_ssl_verify = true
shield_provisioning_key = 'autoprovision'

shield_agent_port = '5444'
shield_agent_advertise_ip = ''
shield_agent_advertise_port = ''

# backups_schedule = 'daily'
# backups_schedule_value = 'daily 4am'
#
# backups_retention = 'shortterm'
# backups_retention_value = '864000'
#
# backups_store = 'default'
